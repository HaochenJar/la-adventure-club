# ─────────────────────────────────────────────────────────────────────────
#  Copy this file to  tools/.env  and fill in your service_role key.
#  Find it in:  Supabase → Project Settings → API → "service_role" (secret)
#               (or a "Secret key" sb_secret_… under the new API keys)
#
#  ⚠️  THIS KEY IS A SECRET with full database access.
#      Never commit tools/.env, never share it, never put it in the website.
#      (tools/.env is already in .gitignore.)
# ─────────────────────────────────────────────────────────────────────────
SUPABASE_URL=https://jmeswonsawyphrutjvuv.supabase.co
SUPABASE_SERVICE_KEY=paste-your-service_role-secret-key-here
