/* ============================================================================
 *  LA Adventure Club — backend config
 *  ---------------------------------------------------------------------------
 *  Paste your Supabase project values below. Find them in your Supabase
 *  dashboard under:  Project Settings → API
 *    • Project URL   → SUPABASE_URL
 *    • anon / public key → SUPABASE_ANON_KEY
 *
 *  The anon key is SAFE to publish in a public site — that's what it's for.
 *  Your data is protected by the Row-Level Security rules in schema.sql,
 *  not by hiding this key. (Never paste the "service_role" key here.)
 *
 *  Until you fill these in, the member portal shows a friendly "not set up
 *  yet" message instead of breaking.
 * ========================================================================== */
window.LAAC_CONFIG = {
  SUPABASE_URL: "YOUR_SUPABASE_URL_HERE",          // e.g. https://abcdxyz.supabase.co
  SUPABASE_ANON_KEY: "YOUR_SUPABASE_ANON_KEY_HERE" // long string starting with "eyJ..."
};
